#!/usr/bin/node

const printStatements = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
// For loop to iterate through eaech printStatement
for (let i = 0; i < printStatements.length; i++) {
  console.log(printStatements[i]);
}
